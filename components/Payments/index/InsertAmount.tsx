import React from "react";
import { StyleSheet } from "react-native";

const InsertAmount: React.FC = () => {
    return <></>
}

export default React.memo(InsertAmount);

const styles = StyleSheet.create({});