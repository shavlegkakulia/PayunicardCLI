import React from "react";
import { StyleSheet } from "react-native";

const PaymentOtp: React.FC = () => {
    return <></>
}

export default React.memo(PaymentOtp);

const styles = StyleSheet.create({});